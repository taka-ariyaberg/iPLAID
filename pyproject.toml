[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "iplaid"
version = "0.1.0"
description = "iPLAID — iDOT Protocol Layout & Assay Integration Dispatcher"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
